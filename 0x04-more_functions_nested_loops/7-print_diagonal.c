#include "main.h"
/**
 *
 *
 */
void print_diagonal(int n)
{
  int x;
  int y;
  if (n <= 0)
    {
    _putchar('\n')
      }
    else
      {
	for (x = 0; x < n; x++)
	  {
	    for (y = 0; y < n; y++)
	      {
		if(j == i)
		_putcher('\\');
	      else if (j < i)
		_putcher (' ');
	      }
	    _putchar('\n');
	  }
      }
}
		  
